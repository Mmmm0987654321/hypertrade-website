'use client';\n\nimport { useState } from 'react';\nimport toast from 'react-hot-toast';\n\nexport default function TradingPanel() {\n  const [orderType, setOrderType] = useState<'buy' | 'sell'>('buy');\n  const [tradingType, setTradingType] = useState<'spot' | 'perp'>('spot');\n  const [symbol, setSymbol] = useState('BTC');\n  const [size, setSize] = useState('');\n  const [price, setPrice] = useState('');\n  const [leverage, setLeverage] = useState('1');\n  const [loading, setLoading] = useState(false);\n\n  const handlePlaceOrder = async () => {\n    if (!symbol || !size) {\n      toast.error('Please fill all fields');\n      return;\n    }\n\n    setLoading(true);\n    try {\n      const response = await fetch(`${process.env.NEXT_PUBLIC_BACKEND_URL}/api/order`, {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify({\n          symbol,\n          size: parseFloat(size),\n          side: orderType,\n          isPerp: tradingType === 'perp',\n          price: price ? parseFloat(price) : undefined,\n          leverage: tradingType === 'perp' ? parseInt(leverage) : 1,\n        }),\n      });\n\n      if (response.ok) {\n        toast.success(`${orderType.toUpperCase()} order placed for ${size} ${symbol}`);\n        setSize('');\n        setPrice('');\n      } else {\n        toast.error('Failed to place order');\n      }\n    } catch (error) {\n      toast.error('Error placing order');\n      console.error(error);\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  return (\n    <div className=\"bg-gradient-dark border border-blue-500/20 rounded-lg p-6 max-w-md\">\n      <h2 className=\"text-xl font-bold text-white mb-4\">Place Order</h2>\n\n      <div className=\"flex gap-2 mb-4\">\n        <button\n          onClick={() => setTradingType('spot')}\n          className={`flex-1 py-2 rounded-lg transition ${\n            tradingType === 'spot'\n              ? 'bg-blue-500 text-white'\n              : 'bg-blue-500/20 text-blue-300 hover:bg-blue-500/30'\n          }`}\n        >\n          Spot\n        </button>\n        <button\n          onClick={() => setTradingType('perp')}\n          className={`flex-1 py-2 rounded-lg transition ${\n            tradingType === 'perp'\n              ? 'bg-blue-500 text-white'\n              : 'bg-blue-500/20 text-blue-300 hover:bg-blue-500/30'\n          }`}\n        >\n          Perpetual\n        </button>\n      </div>\n\n      <div className=\"flex gap-2 mb-4\">\n        <button\n          onClick={() => setOrderType('buy')}\n          className={`flex-1 py-2 rounded-lg transition ${\n            orderType === 'buy'\n              ? 'bg-green-500 text-white'\n              : 'bg-green-500/20 text-green-300 hover:bg-green-500/30'\n          }`}\n        >\n          Buy\n        </button>\n        <button\n          onClick={() => setOrderType('sell')}\n          className={`flex-1 py-2 rounded-lg transition ${\n            orderType === 'sell'\n              ? 'bg-red-500 text-white'\n              : 'bg-red-500/20 text-red-300 hover:bg-red-500/30'\n          }`}\n        >\n          Sell\n        </button>\n      </div>\n\n      <div className=\"mb-4\">\n        <label className=\"text-gray-400 text-sm mb-2 block\">Symbol</label>\n        <input\n          type=\"text\"\n          value={symbol}\n          onChange={(e) => setSymbol(e.target.value.toUpperCase())}\n          placeholder=\"e.g., BTC\"\n          className=\"w-full bg-blue-500/10 border border-blue-500/30 rounded-lg px-3 py-2 text-white placeholder-gray-500 focus:outline-none focus:border-blue-500\"\n        />\n      </div>\n\n      <div className=\"mb-4\">\n        <label className=\"text-gray-400 text-sm mb-2 block\">Size</label>\n        <input\n          type=\"number\"\n          value={size}\n          onChange={(e) => setSize(e.target.value)}\n          placeholder=\"0.00\"\n          className=\"w-full bg-blue-500/10 border border-blue-500/30 rounded-lg px-3 py-2 text-white placeholder-gray-500 focus:outline-none focus:border-blue-500\"\n        />\n      </div>\n\n      <div className=\"mb-4\">\n        <label className=\"text-gray-400 text-sm mb-2 block\">Price (Limit)</label>\n        <input\n          type=\"number\"\n          value={price}\n          onChange={(e) => setPrice(e.target.value)}\n          placeholder=\"0.00\"\n          className=\"w-full bg-blue-500/10 border border-blue-500/30 rounded-lg px-3 py-2 text-white placeholder-gray-500 focus:outline-none focus:border-blue-500\"\n        />\n      </div>\n\n      {tradingType === 'perp' && (\n        <div className=\"mb-4\">\n          <label className=\"text-gray-400 text-sm mb-2 block\">Leverage</label>\n          <select\n            value={leverage}\n            onChange={(e) => setLeverage(e.target.value)}\n            className=\"w-full bg-blue-500/10 border border-blue-500/30 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-blue-500\"\n          >\n            <option value=\"1\">1x</option>\n            <option value=\"2\">2x</option>\n            <option value=\"5\">5x</option>\n            <option value=\"10\">10x</option>\n            <option value=\"20\">20x</option>\n          </select>\n        </div>\n      )}\n\n      <button\n        onClick={handlePlaceOrder}\n        disabled={loading}\n        className={`w-full py-3 rounded-lg font-semibold transition ${\n          orderType === 'buy'\n            ? 'bg-green-500 hover:bg-green-600 text-white'\n            : 'bg-red-500 hover:bg-red-600 text-white'\n        } disabled:opacity-50 disabled:cursor-not-allowed`}\n      >\n        {loading ? 'Processing...' : `${orderType.toUpperCase()} ${symbol}`}\n      </button>\n    </div>\n  );\n}\n