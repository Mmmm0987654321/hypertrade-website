'use client';\n\nimport Link from 'next/link';\nimport { usePrivy } from '@privy-io/react-auth';\nimport { useWalletStore } from '@/store/useWalletStore';\n\nexport default function Navbar() {\n  const { login, logout, authenticated, user } = usePrivy();\n  const { address } = useWalletStore();\n\n  return (\n    <nav className=\"bg-gradient-dark border-b border-blue-500/20 sticky top-0 z-50\">\n      <div className=\"max-w-7xl mx-auto px-4 sm:px-6 lg:px-8\">\n        <div className=\"flex justify-between items-center h-16\">\n          <Link href=\"/\" className=\"flex items-center space-x-2\">\n            <div className=\"text-2xl font-bold bg-gradient-primary bg-clip-text text-transparent\">\n              HyperTrade\n            </div>\n          </Link>\n\n          <div className=\"hidden md:flex space-x-8\">\n            <Link href=\"/\" className=\"text-gray-300 hover:text-white transition\">\n              Dashboard\n            </Link>\n            <Link href=\"/trade\" className=\"text-gray-300 hover:text-white transition\">\n              Trade\n            </Link>\n            <Link href=\"/positions\" className=\"text-gray-300 hover:text-white transition\">\n              Positions\n            </Link>\n            <Link href=\"/wallet\" className=\"text-gray-300 hover:text-white transition\">\n              Wallet\n            </Link>\n          </div>\n\n          <div className=\"flex items-center space-x-4\">\n            {authenticated && address && (\n              <div className=\"text-sm text-gray-300 hidden sm:block\">\n                {address.slice(0, 6)}...{address.slice(-4)}\n              </div>\n            )}\n            {authenticated ? (\n              <button\n                onClick={logout}\n                className=\"px-4 py-2 bg-red-500/20 hover:bg-red-500/30 border border-red-500 text-red-400 rounded-lg transition text-sm\"\n              >\n                Disconnect\n              </button>\n            ) : (\n              <button\n                onClick={login}\n                className=\"px-4 py-2 bg-gradient-primary hover:opacity-90 text-white rounded-lg transition text-sm\"\n              >\n                Connect Wallet\n              </button>\n            )}\n          </div>\n        </div>\n      </div>\n    </nav>\n  );\n}\n